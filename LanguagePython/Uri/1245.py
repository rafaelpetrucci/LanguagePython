while True:
    pe = list()
    try:
        n = int(input())
        pares = count = 0
        while count < n:
            tamanho = len(pe)
            sapatoStr = str(input()).strip().upper()
            sapato = sapatoStr.split()
            if count == 0:
                pe.extend(sapato)
            elif
            count += 1
        pe.clear()
        print(pares)
    except EOFError:
        break
